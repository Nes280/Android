<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.niels.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_perso"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Inscription"
            android:label="@string/title_activity_inscription"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Accueil"
            android:label="@string/title_activity_accueil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DetailsActivity"
            android:label="@string/title_activity_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Accueil_Utilisateur"
            android:label="@string/user_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AjoutActivite"
            android:label="@string/title_activity_ajout_activite"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AjoutEvenement"
            android:label="@string/title_activity_ajout_evenement"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Modification_Profil"
            android:label="@string/title_activity_modification__profil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ChangementPassword"
            android:label="@string/title_activity_changement_password"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AutresActivites"
            android:label="@string/title_activity_autres_activites"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapEvenement"
            android:label="@string/title_activity_details_event" />
        <!--
             ATTENTION: This was auto-generated to add Google Play services to your project for
             App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".DetailsEvenement"
            android:label="@string/title_activity_details_evenement"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ListeEvenements"
            android:label="@string/title_activity_liste_evenements"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AjoutCommentaire"
            android:label="@string/title_activity_ajout_commentaire"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
